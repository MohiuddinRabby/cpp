#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int numOne = 200, numTwo=30;
    cout << "Min: " <<min(numOne,numTwo);
    cout << "\nMax: " << max(numOne,numTwo);
    cout << "\nRound: " << round(5.3);
    cout << "\nFloor: " << floor(56.98);
    return 0;
}
